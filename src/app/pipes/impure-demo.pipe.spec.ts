import { ImpureDemoPipe } from './impure-demo.pipe';

describe('ImpureDemoPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpureDemoPipe();
    expect(pipe).toBeTruthy();
  });
});
